.pio/
.vscode/
secrets.h
.DS_Store

# Secrets / credentials (never commit these)
**/secrets.h
*.pem
*.key
id_rsa
.env
.env.*

# Personal documents / PII (invoices, receipts, ID)
*Invoice*.pdf
DIYElectronics_Invoice_*.pdf
# Photos: HEIC carries EXIF GPS/location metadata
*.HEIC
*.heic
